Apply these files into your repository root, replacing existing files where paths overlap.

Suggested commands after extracting:
python -m pytest tests/test_core_renamer.py tests/test_cli_rename_v1.py
python -m pytest
git add .
git commit -m "add rename apply v1 with first rename idempotence rules"
git push origin main
