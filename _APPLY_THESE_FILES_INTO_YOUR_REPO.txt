Unpack this ZIP into the repository root and overwrite existing files.

Then run:

python -m pytest tests/test_core_workflow_presets_v1.py tests/test_cli_workflow_presets_v1.py
python -m pytest
git add .
git commit -m "add workflow presets and profile previews v1"
git push origin main
