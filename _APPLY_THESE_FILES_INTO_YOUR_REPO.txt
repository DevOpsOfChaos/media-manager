Apply these files into the repository root and replace existing files when prompted.

After unpacking this ZIP, refresh dependencies because Pillow was added:

python -m pip install -e ".[dev]"
python -m pytest tests/test_similar_images.py tests/test_cli_duplicates.py
python -m pytest
