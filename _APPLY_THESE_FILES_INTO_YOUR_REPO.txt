Extract these files into the repository root and replace existing files when prompted.
Then run:
python -m pytest tests/test_core_organizer.py tests/test_cli_organize_v1.py
python -m pytest
git add .
git commit -m "add organize apply v1 with first idempotence rules"
git push origin main
