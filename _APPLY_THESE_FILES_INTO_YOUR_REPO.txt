Apply these files into your repo root and replace existing files where paths match.

Then run:

  python -m pytest tests/test_core_execution_journal.py tests/test_core_undo.py tests/test_cli_undo_v1.py
  python -m pytest
  git add .
  git commit -m "add undo command v1 and loadable execution journals"
  git push origin main
