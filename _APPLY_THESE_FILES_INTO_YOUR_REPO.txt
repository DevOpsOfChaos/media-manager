Apply this update into the root of your media-manager repository.

Included in ZIP 010:
- duplicate session/state cleanup v1
- richer duplicate session metadata
- explicit session restore status/reason reporting
- CLI support for --show-session
- JSON report session block
- tests and docs for the session/state behavior

Suggested steps:
1. Extract this ZIP into your repo root and replace existing files.
2. Run:
   python -m pytest tests/test_duplicate_session_store.py tests/test_cli_duplicates.py
   python -m pytest
3. Commit and push if green.
