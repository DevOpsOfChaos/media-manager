Apply these files into your repository root and overwrite existing files where paths match.

Then run:

python -m pytest tests/test_cli_shell_v1.py
python -m pytest

git add .
git commit -m "add gui shell scaffold v1 for workflow discovery"
git push origin main
