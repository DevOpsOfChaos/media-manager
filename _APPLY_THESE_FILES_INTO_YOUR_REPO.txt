Apply these files into your repository root and replace existing files when asked.

Recommended verification:
1. python -m pytest tests/test_cli_workflow_v1.py
2. python -m pytest

Then commit:
git add .
git commit -m "add workflow shell v1 for guided cli entry points"
git push origin main
