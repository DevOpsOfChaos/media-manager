Copy these files into your repo and replace existing files when prompted.

Then run:

python -m pytest tests/test_core_workflow_forms_v2.py tests/test_cli_shell_forms_v2.py
python -m pytest

after that:

git add .
git commit -m "add gui profile launcher and form binding v2"
git push origin main
